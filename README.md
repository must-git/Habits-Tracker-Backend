# Habits-Tracker-Backend
Be sure to install all the dependencies by running this : pip install -r requirements.txt
Also run this after download MongoDB and Python in the Rest of dependencies; To create the Databases : python manage.py migrate
